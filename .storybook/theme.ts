import { create } from "@storybook/theming";

export default create({
  base: "light",

  // brand
  brandTitle: "Nolus Storybook",
  brandUrl: "/",
  brandImage: "/logo.svg",
  brandTarget: "_self"
});
